# 高级类型与工具类型

## 1. typeof 类型操作符

JavaScript 中 `typeof` 返回运行时类型字符串：

```ts
console.log(typeof "Hello TS"); // "string"
```

TypeScript 中 `typeof` 也可以出现在类型位置，用于获取变量或属性的类型：

```ts
const point = { x: 1, y: 2 };

function formatPoint(value: typeof point) {
  console.log(value.x, value.y);
}

let x: typeof point.x; // number
```

也可以配合函数：

```ts
function add(a: number, b: number) {
  return a + b;
}

type AddFn = typeof add; // (a: number, b: number) => number
type AddReturn = ReturnType<typeof add>; // number
```

教程中“`typeof` 不能获取函数类型”的说法不准确。准确说法是：类型位置的 `typeof` 可以获取变量或属性的类型；函数名如果是一个值，也可以用 `typeof fn` 获取函数类型。但不能直接对任意函数调用表达式使用 `typeof add(1, 2)` 作为类型。

## 2. keyof

`keyof` 获取对象类型的所有键，并组成联合类型：

```ts
type Person = {
  name: string;
  age: number;
};

type Keys = keyof Person; // "name" | "age"
```

如果有字符串索引签名，`keyof` 可能得到 `string | number`，因为 JavaScript 对象属性可以通过数字键访问并转为字符串。

## 3. 索引访问类型

索引访问类型用于获取属性值类型：

```ts
type Props = {
  a: number;
  b: string;
  c: boolean;
};

type A = Props["a"]; // number
type AB = Props["a" | "b"]; // number | string
type Values = Props[keyof Props]; // number | string | boolean
```

数组元素类型：

```ts
const users = [
  { name: "Alice", age: 15 },
  { name: "Bob", age: 23 }
];

type User = (typeof users)[number];
type Age = User["age"];
```

## 4. 映射类型

映射类型基于一组键创建新对象类型：

```ts
type Props = {
  a: number;
  b: string;
  c: boolean;
};

type ToBoolean<T> = {
  [K in keyof T]: boolean;
};

type BooleanProps = ToBoolean<Props>;
```

映射类型只能用于 `type`，不能直接写在 `interface` 中：

```ts
type Keys = "x" | "y";
type Point = {
  [K in Keys]: number;
};
```

## 5. 映射修饰符

添加或移除可选：

```ts
type MyPartial<T> = {
  [K in keyof T]?: T[K];
};

type MyRequired<T> = {
  [K in keyof T]-?: T[K];
};
```

添加或移除只读：

```ts
type MyReadonly<T> = {
  readonly [K in keyof T]: T[K];
};

type Mutable<T> = {
  -readonly [K in keyof T]: T[K];
};
```

## 6. key remapping

TypeScript 支持在映射类型中用 `as` 重映射键：

```ts
type Getters<T> = {
  [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K];
};

type User = {
  name: string;
  age: number;
};

type UserGetters = Getters<User>;
// {
//   getName: () => string;
//   getAge: () => number;
// }
```

也可以过滤键：

```ts
type RemoveKind<T> = {
  [K in keyof T as Exclude<K, "kind">]: T[K];
};
```

## 7. 条件类型

条件类型形式：

```ts
type IsString<T> = T extends string ? true : false;

type A = IsString<string>; // true
type B = IsString<number>; // false
```

条件类型常用来根据输入类型生成不同结果：

```ts
type MessageOf<T> = T extends { message: unknown } ? T["message"] : never;

type Email = {
  message: string;
};

type EmailMessage = MessageOf<Email>; // string
```

## 8. infer

`infer` 用在条件类型里，用于推断某个局部类型变量：

```ts
type MyReturnType<T> = T extends (...args: any[]) => infer R ? R : never;

function getUser() {
  return { name: "jack", age: 18 };
}

type User = MyReturnType<typeof getUser>;
```

数组元素类型：

```ts
type ElementType<T> = T extends Array<infer Item> ? Item : never;

type A = ElementType<string[]>; // string
```

## 9. 分布式条件类型

当条件类型作用在联合类型上时，会分发到每个成员：

```ts
type ToArray<T> = T extends any ? T[] : never;

type A = ToArray<string | number>; // string[] | number[]
```

如果不想分发，可以用元组包住：

```ts
type ToArrayNonDist<T> = [T] extends [any] ? T[] : never;

type B = ToArrayNonDist<string | number>; // (string | number)[]
```

## 10. 模板字面量类型

模板字面量类型可以基于字符串字面量组合新类型：

```ts
type Lang = "en" | "zh";
type Page = "home" | "user";

type LocaleKey = `${Lang}_${Page}`;
// "en_home" | "en_user" | "zh_home" | "zh_user"
```

常用于事件名、接口字段转换：

```ts
type ChangedEvent<T> = `${string & keyof T}Changed`;

type User = {
  name: string;
  age: number;
};

type UserEvent = ChangedEvent<User>; // "nameChanged" | "ageChanged"
```

## 11. 常用内置工具类型

对象转换：

```ts
type A = Partial<User>;
type B = Required<User>;
type C = Readonly<User>;
type D = Pick<User, "name">;
type E = Omit<User, "age">;
type F = Record<"a" | "b", number>;
```

联合类型过滤：

```ts
type A = Exclude<"a" | "b" | "c", "a">; // "b" | "c"
type B = Extract<"a" | "b" | "c", "a" | "b">; // "a" | "b"
type C = NonNullable<string | null | undefined>; // string
```

函数和构造函数：

```ts
type P = Parameters<(name: string, age: number) => void>; // [name: string, age: number]
type R = ReturnType<() => string>; // string

class Person {
  constructor(name: string) {}
}

type CP = ConstructorParameters<typeof Person>; // [name: string]
type I = InstanceType<typeof Person>; // Person
```

Promise：

```ts
type A = Awaited<Promise<string>>; // string
type B = Awaited<Promise<Promise<number>>>; // number
```

字符串工具：

```ts
type A = Uppercase<"hello">; // "HELLO"
type B = Lowercase<"HELLO">; // "hello"
type C = Capitalize<"hello">; // "Hello"
type D = Uncapitalize<"Hello">; // "hello"
```

## 12. 本章融合来源

本章融合了：

- `typescript_guigu/js-ts.md` 中的 `typeof`、索引类型、`keyof`、工具类型。
- `typescript黑马/day-03/26-泛型（工具类型）.ts` 到 `35-索引查询类型的其他使用方式.ts`。
- TypeScript 官方文档的 indexed access types、mapped types、conditional types、template literal types、utility types。
