# 声明文件与 JS 类型补充

## 1. 什么是 .d.ts 文件

`.d.ts` 是 TypeScript 声明文件，用来描述已经存在的 JavaScript 代码的类型。它只提供类型声明，不提供运行时代码实现。

黑马教程中提到：`.d.ts` 文件里不能写可执行代码，这是正确的。

```ts
// index.d.ts
type Point = {
  x: number;
  y: number;
};

declare function add(a: number, b: number): number;
```

不应该写：

```ts
// 错误：声明文件里不写实现
function add(a: number, b: number) {
  return a + b;
}
```

## 2. TS 的两种文件类型

`.ts` 文件可以包含类型和可执行代码：

```ts
type Props = { a: number; b: string };

function add(a: number, b: number) {
  return a + b;
}
```

`.d.ts` 文件只包含类型声明：

```ts
type Props = { x: number; y: number };
export type { Props };
```

## 3. 内置声明文件

TypeScript 自带很多内置声明文件，例如：

- `lib.dom.d.ts`：`window`、`document`、DOM API。
- `lib.es2020.d.ts`：ES2020 标准 API。
- `lib.esnext.d.ts`：更新阶段的标准 API。

这也是为什么你在 TS 中能直接写：

```ts
document.querySelector("#root");
window.location.href;
```

这些类型不是你自己定义的，而是由 `lib` 相关声明文件提供。

## 4. 第三方库类型

第三方库类型来源通常有两种：

1. 库自己携带类型声明，并在 `package.json` 中通过 `types` 或 `typings` 指向入口声明文件。
2. 库没有内置类型，需要安装 `@types/*`。

例如：

```bash
npm install lodash
npm install -D @types/lodash
```

React、Node 等也常见对应类型包：

```bash
npm install -D @types/node
npm install -D @types/react @types/react-dom
```

如果库已经内置类型，就不需要再安装 `@types`。

## 5. 给本地 JS 文件添加类型

假设有 `utils.js`：

```js
export let count = 10;
export let songName = "痴心绝对";

export function add(x, y) {
  return x + y;
}

export function changeDirection(direction) {
  console.log(direction);
}
```

可以创建同目录 `utils.d.ts`：

```ts
export declare let count: number;
export declare let songName: string;

export declare function add(x: number, y: number): number;

export declare function changeDirection(
  direction: "up" | "down" | "left" | "right"
): void;
```

然后在 TS 中使用：

```ts
import { count, songName, add } from "./utils";

console.log(count, songName, add(1, 2));
```

## 6. 声明变量、函数、类、接口

变量：

```ts
declare const version: string;
declare let count: number;
```

函数：

```ts
declare function add(a: number, b: number): number;
```

函数重载：

```ts
declare function add(a: number, b: number): number;
declare function add(a: string, b: string): string;
```

接口：

```ts
export interface Position {
  x: number;
  y: number;
}
```

类：

```ts
declare class Student {
  name: string;
  age: number;
  say(): void;
}
```

## 7. CommonJS 模块声明

如果 JS 使用 CommonJS：

```js
// a.js
let xa = 100;

function sayHi(msg) {
  console.log(msg);
}

module.exports = { xa, sayHi };
```

在真实项目中，更推荐给相对 JS 文件写同名 `.d.ts` 并导出：

```ts
// a.d.ts
export declare const xa: number;
export declare function sayHi(msg: string): void;
```

如果是声明一个包名模块，可以写：

```ts
declare module "hello" {
  export const xa: number;
  export function sayHi(msg: string): void;
}
```

如果模块本身导出一个函数，并使用 `module.exports = fn`：

```ts
declare function app(port: number): void;
export = app;
```

使用时：

```ts
import app = require("app");
app(3000);
```

## 8. 全局声明和模块声明

如果 `.d.ts` 文件没有任何顶层 `import` 或 `export`，里面的声明通常是全局声明：

```ts
// global.d.ts
declare const APP_VERSION: string;
```

如果有顶层 `export`，它就是模块声明文件：

```ts
// types.d.ts
export type Point = {
  x: number;
  y: number;
};
```

在模块声明文件中扩展全局类型，需要用 `declare global`：

```ts
export {};

declare global {
  interface Window {
    appVersion: string;
  }
}
```

## 9. 模块扩展

可以扩展已有模块中的类型：

```ts
// test1.d.ts
export interface A {
  a: string;
}
```

```ts
// test2.d.ts
import "./test1";

declare module "./test1" {
  interface A {
    b: string;
  }
}
```

这依赖 TypeScript 的声明合并机制，常用于扩展第三方库类型。

## 10. 在 JS 项目中逐步使用 TS

如果项目暂时还是 JavaScript，可以使用：

```json
{
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true
  }
}
```

也可以在 JS 文件中使用 JSDoc：

```js
/**
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
function add(a, b) {
  return a + b;
}
```

迁移策略：

- 先启用 `allowJs`。
- 对关键 JS 文件启用 `checkJs` 或局部 `// @ts-check`。
- 给第三方或本地 JS 模块补 `.d.ts`。
- 逐步把核心文件从 `.js` 改成 `.ts`。

## 11. 本章融合来源

本章融合了：

- `typescript_guigu/js-ts.md` 中 `.d.ts` 文件说明、第三方库类型、本地 JS 类型补充、模块扩展。
- `typescript黑马/day-04/01-TypeScript 类型声明文件概述` 到 `06-为已有的JS文件提供类型声明`。
- TypeScript 官方文档关于 declaration files、declaration merging、TSConfig `allowJs/checkJs` 的说明。
