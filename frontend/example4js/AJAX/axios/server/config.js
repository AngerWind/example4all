export const port = 9999;
export const url = "/api/resource"

export let requestUrl = `http://localhost:${port}${url}`

export let fileUpload = '/fileUpload'
export let upload = `http://localhost:${port}${fileUpload}`

export let fileDownload = '/fileDownload'
export let download = `http://localhost:${port}${fileDownload}`